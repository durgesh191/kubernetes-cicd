name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: durgesh191
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and Push Docker image
        run: |
          docker build -t ghcr.io/durgesh191/my-app1:latest -f docker/Dockerfile .
          docker push ghcr.io/durgesh191/my-app1:latest
